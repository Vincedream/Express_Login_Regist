<!DOCTYPE html>
<html>
  <head>
    <title>index</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>This is Index Page</h1>
    <% if(user && user.username) { %>
    <p><%= user.username %>你好，欢迎来到 Index</p>
    <a href="/logout">退出</a>
    <% }else{ %>
    <p>您还没有登陆，请登陆之后再操作</p>
    <a href="/login">登陆</a>
    <a href="/register">注册</a>
    <% } %>
  </body>
</html>
